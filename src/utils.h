#include <cstdlib>
#include <cstdint>
#include <fstream>
#include <iostream>
#include <random>
#include <string>
#include <vector>

#define loop for (;;)
#define Screen std::vector<std::vector<byte>>
typedef uint8_t byte;
typedef uint16_t word;
